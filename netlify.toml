# Netlify configuration for SPA
# Alternative to _redirects file
# Handles client-side routing for React Router

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

[build]
  publish = "dist"
  command = "npm run build"

[dev]
  command = "npm run dev"
